name: meridian-affinity
version: 1.0.0
description: "Meridian integration: injects x-session-affinity (prompt-cache continuity), x-request-id, x-meridian-source and x-opencode-effort on every LLM request; exposes the usage_status tool (quota windows) back to the agent."
